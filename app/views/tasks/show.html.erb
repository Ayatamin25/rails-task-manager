<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Back to list", tasks_path%>
<%= link_to "Edit Task", edits_path%>

<%= link_to "Delete task", delete_path(@task),
 data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
